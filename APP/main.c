#include <stdio.h>
#include <stdlib.h>
#include "PRINTS/print.h"

int main(argc, ** argv)
{
    //Get data from the saved file.
    int isFirstGame = 1; //testing
    welcomeToPoCmon();
    if (isFirstGame == 1){
        //start the game
        firstGame();
    }

    //Display
}